sudo docker exec -it my-postgres psql -U postgres -- подключение к БДшке


запуск MiniO

docker run -d \
  -p 9000:9000 \
  -p 9001:9001 \
  --name minio-local \
  -e "MINIO_ROOT_USER=root" \
  -e "MINIO_ROOT_PASSWORD=SixSeven" \
  minio/minio server /data --console-address ":9001"



Лозунг: "Walls как место, где процессы людей лежат рядом, и ты можешь читать не чужой образ, а чужой путь."

